export const metadata = {
  title: "Login",
  description: "Log in to your Doctor Appointment Manager account to manage appointments and access healthcare services.",
};

export default function LoginLayout({ children }) {
  return children;
}
